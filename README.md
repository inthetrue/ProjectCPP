# Название проекта: Умный телефонный справочник
**Язык программирования:** C++

## 🎯 Цель проекта
Разработка программного обеспечения для организации и ведения базы контактов. Программа обеспечивает хранение данных в оперативной памяти с возможностью сохранения во внешний файл, поддерживает валидацию вводимых данных, поиск и сортировку.

## 📦 Описание
Программа представляет собой консольное приложение на C++, которое работает с базой контактов (Имя, Телефон, Email). Данные хранятся в динамическом массиве (`std::vector`), что позволяет добавлять неограниченное количество записей (в пределах оперативной памяти).

### ✅ Функционал
* **Умное добавление** — ввод данных со строгой валидацией (защита от ввода цифр в имени, букв в телефоне и т.д.).
* **Редактирование и удаление** — возможность изменить или удалить любую запись по индексу.
* **Поиск контактов** — нахождение записей по части имени (без учета регистра).
* **Сортировка** — упорядочивание списка по алфавиту (А-Я) и в обратном порядке (Я-А) методом пузырька.
* **Сохранение и загрузка** — работа с текстовыми файлами для переноса базы данных.
* **Безопасность данных** — защита от сохранения пустого списка и проверка файлов при загрузке.

## 🧩 Используемые технологии
* `iostream` — ввод/вывод данных через консоль.
* `vector` — динамическое хранение списка контактов.
* `string` — работа со строковыми данными.
* `fstream` — чтение и запись файлов базы данных.
* **Алгоритмы:** Пузырьковая сортировка, линейный поиск, ручная валидация ASCII-кодов (без использования `<cctype>`).

## 🖥️ Инструкция по запуску

  ### Шаг 1. Скачивание проекта
  Скачайте архив с исходным кодом проекта или склонируйте репозиторий.

  ### Шаг 2. Запуск программы
  Для `Windows`:
  Запустите созданный файл Phonebook.exe двойным щелчком или через командную строку.

  Для `Linux/macOS`:
  Откройте терминал в папке с программой.
  Дайте права на выполнение (если нужно): chmod +x Phonebook
  Запустите: ./Phonebook

  ### Шаг 4. Использование программы
  После запуска вы увидите меню с опциями. Выберите нужный пункт, вводя соответствующую цифру и нажимая Enter.

## 🔢 Интерфейс программы
Главное меню приложения:

  1. Добавить контакт — Ввод имени, телефона, email с автоматической проверкой.
  2. Показать все контакты — Вывод пронумерованного списка на экран.
  3. Поиск по имени — Найти контакт по подстроке (например, по "Ivan" найти "Ivanov").
  4. Редактировать контакт — Изменение полей существующей записи.
  5. Удалить контакт — Удаление записи по её порядковому номеру.
  6. Сортировка — Выбор режима сортировки: А-Я или Я-А.
  7. Сохранить в файл — Запись текущей базы в файл phonebook_data.txt.
  8. Загрузить из файла — Чтение данных из файла phonebook_data.txt.
  0. Выход — Завершение работы программы.

## 💾 Формат файла для сохранения/загрузки
Контакты сохраняются в текстовый файл (по умолчанию phonebook_data.txt) в строгом формате, удобном для парсинга.

### Структура файла:
  1. Первая строка: Количество записей (целое число).
  2. Далее идут блоки по 3 строки на каждый контакт:
    * Строка с именем
    * Строка с телефоном
    * Строка с email

Пример содержимого файла:
`2
Иван Иванов
89001234567
ivan@mail.ru
Петр Петров
+7(999)111-22-33
petr@gmail.com`

## 💡 Особенности и ограничения
  1. **Валидация имени**: Нельзя вводить цифры и спецсимволы. Разрешены только русские/английские буквы и пробелы.
  2. **Валидация телефона**: Нельзя вводить буквы. Разрешены цифры, скобки, плюс, дефис. Можно ввести -, если телефона нет.
  3. **Валидация Email**: Обязательно наличие символа @. Кириллица запрещена. Можно ввести -, если почты нет.
  4. **Сортировка**: Используется алгоритм пузырька с временной сложностью $O(N^2)$, что эффективно для списков до нескольких тысяч записей.
